---
name: build-deck
description: Create, revise, or structure PowerPoint-first presentations for course projects; legacy Beamer is explicit opt-in
---

# Build Deck Skill

Read `docs/ai/workflows/build-deck.md` and follow it as the canonical
workflow.

PowerPoint is the default deck format. Do not use LaTeX, Beamer, or
`pdflatex` unless the student explicitly asks for legacy `.tex` slides.

If this wrapper conflicts with the shared workflow doc, the shared workflow
doc wins.
