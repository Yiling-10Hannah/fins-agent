---
name: proofread
description: Mechanical error scan for Word/.docx report prose by default; legacy LaTeX is supported only when an explicit .tex target is supplied
---

# Proofread Skill

Read `docs/ai/workflows/proofread.md` and follow it as the canonical
workflow.

If this wrapper conflicts with the shared workflow doc, the shared workflow
doc wins.
