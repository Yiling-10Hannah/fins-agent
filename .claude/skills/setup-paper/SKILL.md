---
name: setup-paper
description: "Scaffold a new course report, creating Word report/report.docx by default or legacy LaTeX only when explicitly requested"
argument-hint: "<title> [--authors 'Name (Student ID, Course Code)'] [--topic 'one-line description']"
user_invocable: true
---

# Setup Paper

Read `docs/ai/workflows/setup-paper.md` and follow it as the canonical
workflow.

If this wrapper conflicts with the shared workflow doc, the shared workflow
doc wins.
