description = "Create a Word report scaffold by default, with legacy LaTeX opt-in."
prompt = """
Follow the shared repo workflow below as the canonical instructions for this task.

@{docs/ai/workflows/setup-paper.md}

Use this command when a student asks to set up a paper, create a Word
report, create report/report.docx, or copy the report boilerplate.

Preferred deterministic command:
- `python tools/workflow.py setup-paper --title "..." [--authors "..."] [--topic "..."] [--target .]`
- `python tools/workflow.py setup-paper --format latex --title "..." [--target .]`

The repo instructions from GEMINI.md and AGENTS.md are already loaded.
Treat any text appended after this prompt as the user's paper setup request.
If the shared workflow doc conflicts with older tool-specific guidance, the shared workflow doc wins.
"""
